# Copy to .env and fill in real values. Nothing in this repo works with
# fabricated/placeholder secrets — see saas/README.md for what each one
# actually requires and how to obtain it.

NODE_ENV=development
PORT=4000

# Local Postgres started via `saas/backend/scripts/dev-db.sh` uses this by
# default; point at a managed instance (RDS/Supabase/Neon/...) in production.
DATABASE_URL=postgresql://localhost:5432/preny_clone

# Any long random strings — used to sign JWTs. Rotate for production;
# rotating invalidates all existing sessions.
JWT_ACCESS_SECRET=change-me-access
JWT_REFRESH_SECRET=change-me-refresh
ACCESS_TOKEN_TTL=15m
REFRESH_TOKEN_TTL_DAYS=30

# Must match the frontend's real origin (credentials: true cookies require
# an exact match, not a wildcard).
CORS_ORIGIN=http://localhost:5173

# Real key from https://console.anthropic.com — required for the chatbot to
# generate real replies. Left unset, /api/widget/message and the Facebook
# webhook still work end-to-end but the bot step is skipped with a visible
# "AI provider not configured" event instead of a fabricated reply.
ANTHROPIC_API_KEY=

# Real values from a real Meta App (developers.facebook.com) with the
# Messenger product added. Required for /api/fanpages/connect/facebook and
# the /api/webhooks/facebook signature check to do anything; without them
# those endpoints return a clear 501/403 rather than pretending to work.
FACEBOOK_APP_ID=
FACEBOOK_APP_SECRET=
FACEBOOK_WEBHOOK_VERIFY_TOKEN=

# Optional. Without these, uploaded images (dashboard + widget) are saved
# to this server's local disk at /uploads/* — fine for development and for
# the Claude vision tool call (images are base64-inlined, see
# ai.service.js), but NOT reachable by Facebook's servers, so a bot's
# image reply can never actually reach a real Messenger customer. Setting
# both switches uploads to S3 automatically (see storage.service.js) with
# no other code changes needed. S3_PUBLIC_BASE_URL is your CloudFront
# distribution domain or the bucket's public website endpoint — whatever
# an anonymous HTTP GET can actually reach.
S3_BUCKET=
AWS_REGION=us-east-1
S3_PUBLIC_BASE_URL=

# Real values from https://dashboard.stripe.com (test mode is fine for
# development). Required for /api/billing/checkout and /api/billing/portal
# to do anything — without them those endpoints return a clear 501 rather
# than a fabricated checkout URL. STRIPE_WEBHOOK_SECRET comes from the
# webhook endpoint you register (locally: `stripe listen --forward-to
# localhost:4000/api/webhooks/stripe`) pointing at STRIPE_PRICE_ID_GROWTH/
# BUSINESS — real Stripe Price IDs for the Growth/Business plans (see
# prisma/seed.js); leave a plan's Price ID unset and checkout for that
# plan stays disabled rather than guessing a price.
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_ID_GROWTH=
STRIPE_PRICE_ID_BUSINESS=

# The frontend's real origin — Stripe redirects the browser back here
# after checkout/the billing portal, and password-reset emails link here.
FRONTEND_URL=http://localhost:5173

# Real SMTP credentials (e.g. a Mailgun/SES/Postmark SMTP endpoint, or
# Gmail's smtp.gmail.com with an app password). Required for
# /api/auth/forgot-password to actually deliver an email — without them
# the reset link is logged server-side instead (see email.service.js) and
# the API still responds the same generic "check your email" message
# either way, so an anonymous caller can't use it to learn whether email
# delivery is configured or whether a given address has an account.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=no-reply@preny-clone.example
